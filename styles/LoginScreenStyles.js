import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    loginContainer: {
        width: '100%',
        alignItems: 'center',
        marginTop: 80
    },
    inputField: {
        width: '80%',
        backgroundColor: '#fff',
        borderRadius: 50,
        marginBottom: 80,
        padding: 20,
        fontSize: 20
    }
})