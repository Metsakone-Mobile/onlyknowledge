import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    customButton: {
        backgroundColor: '#42f58a',
        borderRadius: 45,
        marginTop: 5,
        marginBottom: 5,
        width: '50%',
        alignItems: 'center'
    },
    buttonText: {
        fontSize: 16,
        margin: 5,
        fontWeight: 'bold'
    }
})