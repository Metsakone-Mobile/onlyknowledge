import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        marginBottom: 40
    },
    label: {
        fontSize: 32,
    },
    descriptionField: {
        height: 150,
        width: 300,
        padding: 5,
        backgroundColor: '#fff',
        borderRadius: 10,
        borderWidth: 1,
        fontSize:20,
    },
})